import styles from "./styles.module.scss"

const Header = () => {
  return (
    <header className={styles.header}>
      <h1>Station Five</h1>
      <h2 className={styles.header__subheading}>Radio Buttons</h2>
    </header>
  )
}

export default Header
